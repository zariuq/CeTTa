; Valid presentation with the authority rule removed. Generic execution must
; consequently derive no parse for the two-char root.

(gslt-presentation-v1 TwoCharWithoutDefinitionV1
  (signature)
  (equations)
  (rewrites))
