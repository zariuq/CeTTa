; import! supplies a missing .metta extension and keeps an authored one,
; so both spellings name one library.
!(import! &self (library lib_combinatorics.metta))
!(collapse (range 0 3))
!(import! &self (library lib_import.metta))
