; Base PeTTa retains its own collection and search forms, while a CeTTa-only
; space extension remains ordinary data until the explicit extended profile.
!(size (beta alpha))
!(size-atom (beta alpha))
!(collapse (hyperpose (beta alpha)))
