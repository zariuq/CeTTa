; Ordinary Prime judgments are unbounded and unmetered. A trailing positive
; number selects the explicit bounded variant. Exact ledgers are C-internal.

(: Nat Type)
(: Z Nat)
(= (only) Z)

!(type:may (only) Nat)
!(type:may (only) Nat 100)
