; Two distinct directional rule occurrences recognize the same outcome of one
; shared Need cell.  Prime exports occurrence identity and causal receipts; the
; probability bridge assigns the explicit fair prior outside the language.
(= (shared-cause:goal heads) goal)
(= (shared-cause:goal heads) goal)
!(shared-cause:goal (superpose (heads tails)))
