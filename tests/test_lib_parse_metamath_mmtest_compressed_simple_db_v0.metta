; Exploratory external-corpus red frontier:
; `test_compressed_simple.mm` is accepted by mm-lean4 but still OOMs on the
; capped CeTTa side. Keep this as a diagnostic witness until the generic parser
; reaches it.
