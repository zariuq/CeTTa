; Complete-bag observation makes internal branch order invisible.  Until a
; serializable bulk activation is admitted, the profitable exact plan is the
; ordinary inline producer rather than a singleton-scheduled frontier.

(= (controller-diversity:auto-bag) (answer a0))
(= (controller-diversity:auto-bag) (answer a1))
(= (controller-diversity:auto-bag) (answer a2))
(= (controller-diversity:auto-bag) (answer a3))

!(collapse (controller-diversity:auto-bag))
